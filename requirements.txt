# Core dependencies
fastmcp>=0.1.0
pydantic>=2.0.0

# Testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Optional: for development
httpx>=0.24.0

# Advanced audio features (optional)
# Install with: pip install -r requirements-audio.txt
# demucs>=4.0.0          # Voice isolation (Facebook/Meta's neural network)
# pedalboard>=0.9.0      # VST3/VST2 plugin support (Spotify)
# librosa>=0.10.0        # Audio analysis (pitch detection, tempo)
# soundfile>=0.12.0      # Audio file I/O
